<section id="home-newsletter">
    <div class="container">
        <h3>{{ trans('messages.best_deals') }}</h3>
        <p>{{ trans('messages.save_money_best_deals') }}﻿</p>
        <a href="#" class="open-newsletter-modal click-button">{{ trans('messages.subscribe') }}</a>
    </div>
</section>